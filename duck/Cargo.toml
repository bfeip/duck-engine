[workspace]
resolver = "3"
members = [
    "render-core",
    "renderer",
    "viewer",
    "scene",
    "common",
    "import-export",
    "egui-demo",
    "modeler",
]
default-members = ["viewer"]


[profile.small]
inherits = "release"
opt-level = "s"
strip = true
lto = true
codegen-units = 1
